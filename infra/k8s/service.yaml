apiVersion: v1
kind: Service
metadata:
  name: k8s-log-anomaly-triage
  labels:
    app: k8s-log-anomaly-triage
spec:
  selector:
    app: k8s-log-anomaly-triage
  ports:
    - name: http
      port: 80
      targetPort: 8000
