services:
  triage-api:
    build: .
    image: k8s-log-anomaly-triage-service:local
    ports:
      - "8000:8000"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
